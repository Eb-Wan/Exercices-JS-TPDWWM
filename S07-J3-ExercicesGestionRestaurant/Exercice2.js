const waiterName = "Pierrick";

const reservation = {
    clientName: "Kevin",
    clientTable: 5,
    clientPersonNumber: 4
}

console.log (`Bonjour ${waiterName}, la réservation pour ${reservation.clientName} (${reservation.clientPersonNumber} personnes) est confirmée à la table ${reservation.clientTable}.`);